/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package antgame;

import java.util.ArrayList;
import org.junit.After;
import org.junit.AfterClass;
import org.junit.Before;
import org.junit.BeforeClass;
import org.junit.Test;
import static org.junit.Assert.*;

/**
 *
 * @author Jason
 */
public class AntTest {
    ArrayList<Ant> ant = new ArrayList<>();

    public AntTest() {
    }

    @BeforeClass
    public static void setUpClass() {
    }

    @AfterClass
    public static void tearDownClass() {
    }

    @Before
    public void setUp() {
    }

    @After
    public void tearDown() {
    }

    /**
     * Test of other_color method, of class Ant.
     * pass if returning opposite color
     * return RED if its a black ant
     * return BLACK if tis a red ant
     */
    @Test
    public void testOther_color() {
        System.out.println("other_color");
        Color c = Color.RED;
        Cell pos = new Cell(0, 1);        
        Color expResult = Color.BLACK;
        Color result = c.other_color(c);
        assertEquals(expResult, result);
    }

    /**
     * Test of set_state method, of class Ant.
     * pass if the ant able to store the state assigned to it
     */
    @Test
    public void testSet_state() {
        System.out.println("set_state");
        Cell pos = new Cell(0, 1);
        Color c = Color.RED;
        Cell[][] customWorld = new Cell[150][150];
        for (int i = 0; i < 150; i++) {
            for (int j = 0; j < 150; j++) {
                customWorld[i][j] = new Cell(i, j);
            }
        }
        World w = new World(customWorld, ant);
        Ant a = new Ant(w, pos,c);
        int s = 0;
        a.set_state(s);
        int result = a.state();
        assertEquals(s, result);
    }

    /**
     * Test of set_resting method, of class Ant.
     * check if parametre resting is correctly stored
     */
    @Test
    public void testSet_resting() {
        System.out.println("set_resting");
        Cell pos = new Cell(0, 1);
        Color c = Color.RED;
        Cell[][] customWorld = new Cell[150][150];
        for (int i = 0; i < 150; i++) {
            for (int j = 0; j < 150; j++) {
                customWorld[i][j] = new Cell(i, j);
            }
        }
        World w = new World(customWorld, ant);
        Ant a = new Ant(w, pos,c);
        int r = 0;
        a.set_resting(r);
        int result = a.resting();
        assertEquals(r, result);
    }

    /**
     * Test of set_direction method, of class Ant.
     * check if parametre direction is correctly stored
     */
    @Test
    public void testSet_direction() {
        System.out.println("set_direction");
        Cell pos = new Cell(0, 1);
        Color c = Color.RED;
        Cell[][] customWorld = new Cell[150][150];
        for (int i = 0; i < 150; i++) {
            for (int j = 0; j < 150; j++) {
                customWorld[i][j] = new Cell(i, j);
            }
        }
        World w = new World(customWorld, ant);
        Ant a = new Ant(w, pos,c);
        int d = 0;
        a.set_direction(d);
        int result = a.direction();
        assertEquals(d, result);
    }

    /**
     * Test of set_has_food method, of class Ant.
     * check if parametre has_food is correctly updated
     */
    @Test
    public void testSet_has_food() {
        System.out.println("set_has_food");
        Cell pos = new Cell(0, 1);
        Color c = Color.RED;
        Cell[][] customWorld = new Cell[150][150];
        for (int i = 0; i < 150; i++) {
            for (int j = 0; j < 150; j++) {
                customWorld[i][j] = new Cell(i, j);
            }
        }
        World w = new World(customWorld, ant);
        Ant a = new Ant(w, pos,c);
        boolean b = false;
        a.set_has_food(b);
        boolean result = a.has_food();
        assertEquals(b, result);
    }

    /**
     * Test of adjacent_ants method, of class Ant.
     * check if the number of adjecent ant can be found correctly
     */
    @Test
    public void testAdjacent_ants() {
        System.out.println("adjacent_ants");
        Cell p = new Cell(1, 1);
        Cell pos = new Cell(0, 1);
        Color c = Color.RED;
        Cell[][] customWorld = new Cell[150][150];
        for (int i = 0; i < 150; i++) {
            for (int j = 0; j < 150; j++) {
                customWorld[i][j] = new Cell(i, j);
            }
        }
        World w = new World(customWorld, ant);
        Ant a = new Ant(w, pos,c);
        Ant instance = new Ant(w, p,c);
        int expResult = 0;
        int result = w.adjacent_ants(p, c);
        assertEquals(expResult, result);
    }

    /**
     * Test of check_for_surrounded_ant_at method, of class Ant.
     * check can the method check_for_surrounded_ant_at run properly 
     */
    @Test
    public void testCheck_for_surrounded_ant_at() {
        System.out.println("check_for_surrounded_ant_at");
        Cell p = new Cell(5, 5);
        Cell pos = new Cell(5, 6);
        Color c = Color.RED;
        Cell[][] customWorld = new Cell[150][150];
        for (int i = 0; i < 150; i++) {
            for (int j = 0; j < 150; j++) {
                customWorld[i][j] = new Cell(i, j);
            }
        }
        World w = new World(customWorld, ant);
        Ant instance = new Ant(w, p,c);
        w.check_for_surrounded_ant_at(p);
    }

    /**
     * Test of check_for_surrounded_ants method, of class Ant.
     * test if check_for_surrounded_ants method able to get the correct number of ant surrounding the particular cell
     */
    @Test
    public void testCheck_for_surrounded_ants() {
        System.out.println("check_for_surrounded_ants");
        Cell p = new Cell(1, 1);
        Cell pos = new Cell(1, 2);
        Color c = Color.RED;
        Cell[][] customWorld = new Cell[150][150];
        for (int i = 0; i < 150; i++) {
            for (int j = 0; j < 150; j++) {
                customWorld[i][j] = new Cell(i, j);
            }
        }
        World w = new World(customWorld, ant);
        Ant a = new Ant(w, p,c);       
        Ant instance = new Ant(w, pos,c);
        w.check_for_surrounded_ants(pos);
    }

    /**
     * Test of getCell method, of class Ant.
     * check can cell can be stored and retrieved correctly from an ant
     */
    @Test
    public void testGetCell() {
        System.out.println("getCell");
        Cell p = new Cell(0, 0);
        Color c = Color.RED;
        Cell[][] customWorld = new Cell[150][150];
        for (int i = 0; i < 150; i++) {
            for (int j = 0; j < 150; j++) {
                customWorld[i][j] = new Cell(i, j);
            }
        }
        World w = new World(customWorld, ant);   
        AntHill instance = new AntHill(w, p, c);        
        Cell result = instance.getCell();
        System.out.println(result.getX());
        System.out.println(result.getY());
        assertEquals(p, result);
    }

    /**
     * Test of getId method, of class Ant.
     * check if the id of the ant canbe retrieved
     */
    @Test
    public void testGetId() {
        System.out.println("getId");
        Cell p = new Cell(0, 0);
        Color c = Color.RED;
        Cell[][] customWorld = new Cell[150][150];
        for (int i = 0; i < 150; i++) {
            for (int j = 0; j < 150; j++) {
                customWorld[i][j] = new Cell(i, j);
            }
        }
        World w = new World(customWorld, ant);
        Ant instance = new Ant(w,p,c);
        int expResult = 0;
        int result = instance.getId();
        assertEquals(expResult, result);
    }

}
